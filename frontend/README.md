# ChatSphere Frontend

## Setup

1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Place a short notification sound named `notify.mp3` in `frontend/public/` to enable sound notifications.

Environment variables are in `.env` (VITE_API_URL and VITE_SOCKET_URL).
